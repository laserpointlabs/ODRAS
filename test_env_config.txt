# ODRAS Test Environment Configuration
# Used by CI/CD and testing environments

# =============================================================================
# INSTALLATION-SPECIFIC IRI CONFIGURATION (Test)
# =============================================================================
INSTALLATION_NAME=ODRAS-CI-TEST
INSTALLATION_TYPE=test
TOP_LEVEL_DOMAIN=local
INSTALLATION_BASE_URI=https://test.odras.local
INSTALLATION_ORGANIZATION=ODRAS CI Test Environment
INSTALLATION_PROGRAM_OFFICE=Automated Testing
AUTHORITY_CONTACT=ci-test@odras.local

# =============================================================================
# DATABASE CONFIGURATION (Matches CI services)
# =============================================================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DATABASE=odras
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password

# =============================================================================
# STORAGE CONFIGURATION (Disabled for testing)
# =============================================================================
STORAGE_BACKEND=local
LOCAL_STORAGE_PATH=./storage/files

# =============================================================================
# LLM CONFIGURATION (OpenAI for CI - uses GitHub secret)
# =============================================================================
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o-mini
# OPENAI_API_KEY will be set from GitHub secret

# Local development can use Ollama if preferred:
# LLM_PROVIDER=ollama
# LLM_MODEL=llama3.1:8b
# OLLAMA_URL=http://localhost:11434

# =============================================================================
# SERVICE URLS (Match CI service definitions)
# =============================================================================
QDRANT_URL=http://localhost:6333
NEO4J_URL=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=testpassword
FUSEKI_URL=http://localhost:3030/odras
REDIS_URL=redis://localhost:6379
# CAMUNDA_BASE_URL=http://localhost:8080  # Not available in CI
# OLLAMA_URL=http://localhost:11434       # Not available in CI

# =============================================================================
# APPLICATION CONFIGURATION (Test settings)
# =============================================================================
API_BASE_URL=http://localhost:8000
ENVIRONMENT=test
LOG_LEVEL=INFO
SECRET_KEY=test_secret_key_for_ci
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# =============================================================================
# TEST-SPECIFIC SETTINGS
# =============================================================================
# Disable features that require external services
ENABLE_EXTERNAL_APIS=false
ENABLE_TELEMETRY=false
ENABLE_BACKGROUND_WORKERS=false
